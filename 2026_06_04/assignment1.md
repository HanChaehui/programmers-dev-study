# OS

---

**컴퓨터 구조**

- 컴퓨터가 이해하는 정보 : 데이터, 명령어
- 컴퓨터의 네 가지 핵심 부품 : cpu(중앙처리장치), RAM(메모리, 주기억장치), 보조기어장치, 입출력장치

**컴퓨터가 이해하는 정보**

- **데이터** : 컴퓨터가 이해하는 숫자, 문자, 이미지, 동영상과 같은 정적인 정보
- **명령어** : 컴퓨터를 실질적으로 작동 시키는 정보

> 명령어는 컴퓨터를 작동시키는정보이고, 데이터는 명령어를 위해 존재하는 일종의 재료이다.
> 

**컴퓨터의 네 가지 핵심 부품**

**메모리**

- 현재 실행되는 프로그램의 명령어와 데이터를 저장하는 부품이다.
- 프로그램이 실행되려면 반드시 메모리에 저장되어 있어야한다.
- 이때 컴퓨터가 빠르게 작동하기 위해서는 메모리 속 명령어와 데이터 구조가 중구난방으로 저장되어 있으면 안된다. 그래서 메모리에는 저장된 값에 빠르고 효율적으로 접근하기 위해 주소라는 개념이 사용된다.
- 메모리에 저장된 값의 위치는 주소로 알 수 있다.

**CPU**

- 메모리에 저장된 명령어를 읽어 들이고, 읽어 들인 명령어를 해석하고, 실행하는 부품이다.
- CPU의 작동 원리를 구체적으로 이해하기 위해서는 CPU 내부 구성 요소를 알아야 한다.
    - **ALU(산술논리연산장치)** : 컴퓨터 내부에서 수행되는 대부분의 계산을 도맡아 수행
    - **레지스터** : CPU 내부의 작은 임시 저장 장치
    - **제어장치** : 전기 신호를 내보내고 명령어를 해석하는 장치

**레지스터**는 CPU **안에** 있는 아주 작은 임시 저장 공간이다. 개수가 몇십 개뿐이지만 **속도가 극도로 빠릅니다.** **메모리(RAM)** 는 CPU **바깥에** 있는 훨씬 큰 저장 공간이에요. 프로그램의 코드와 데이터를 담고 있고, 레지스터보다 용량이 비교가 안 되게 크지만 **상대적으로 느리다.**

핵심: **CPU는 계산을 "레지스터에 있는 값"으로만 할 수 있다.** 메모리에 있는 데이터를 직접 더하거나 빼지 못한다. 그래서 항상 이 순서로 움직인다:

1. 메모리에서 필요한 값을 레지스터로 **가져온다 (load, 읽기)**
2. 레지스터 안에서 계산한다
3. 결과를 다시 메모리에 **저장한다 (store, 쓰기)**

**보조기억장치**

- 메모리는 가격이 비싸 저장 용량이 적고, 전원이 꺼지면 저장된 내용을 잃는다는 약점이 있다. 이에 메모리보다 크기가 크고 전원이 꺼져도 저장된 내용을 잃지 않는 메모리를 보조할 저장 장치가 필요하게 되었는데, 이 저장 장치가 보조기억장치이다.(하드 디스크, SSD, USB,...)
- 메모리가 현재 '실행되는' 프로그램을 저장한다면, 보조기억장치는 '보관할' 프로그램을 저장한다.

**입출력장치**

- 컴퓨터 외부에 연결되어 컴퓨터 내부와 정보를 교환하는 장치

!운영체제_1.png

**메인보드와 시스템 버스**

- 지금까지 설명한 컴퓨터의 핵심부품들은 모두 메인보드라는 판에 연결된다. 혹은 마더보드라고도 한다.
- 메인보드에 연결된 부품들은 서로 정보를 주고 받을 수 있는데, 이는 메인보드 내부에 버스라는 통로가 있기 때문이다. 컴퓨터 내부에는 다양한 종류의 통로, 즉 버스가 있다. 하지만 여러 버스 가운데 컴퓨터의 네 가지 핵심 부품을 연결하는 가장 중요한 버스는 시스템 버스이다.

# CPU와 레지스터의 관계

---

프로그램이 실행되면 운영체제는 프로그램과 데이터를 메모리에 올린다. 하지만 CPU는 메모리에 있는 값을 바로 계산하지 않고, 필요한 값을 CPU 내부의 레지스터로 가져와서 연산한다.

#### 실행 흐름

운영체제가 프로그램을 메모리에 올리면, CPU는 메모리에서 명령어를 하나씩 가져와 실행한다.

명령어를 실행하는 과정에서 필요한 데이터가 있으면, CPU는 그 데이터를 메모리에서 읽어 **레지스터에 적재**한다.

그 후 CPU는 레지스터에 들어 있는 값을 이용해 계산하고, 계산 결과를 다시 레지스터에 저장하거나 필요하면 메모리에 저장한다.

메모리는 프로그램의 명령어와 데이터를 저장하는 공간이고, 레지스터는 CPU가 연산할 때 사용하는 아주 빠른 임시 저장 공간이다.

즉, **CPU는 메모리의 값을 레지스터로 가져온 뒤, 레지스터에 있는 값을 이용해 연산한다.**

# Git, Github

---

## Git

소스 코드 버전 관리 시스템

* 버전 관리 시스템: 내가 원하는 시점으로 이동이 가능하게 해줌

## Github

Git으로 관리하는 프로젝트를 인터넷에 올려두고 관리 및 공유할 수 있는 대표적인 Git 호스팅 서비스

*Git 호스팅 서비스: Github 이외에도 Gitlab, Bitbucket등이 존재

# JVM, JRE, JDK

---

## JVM (Java Virtual Machine)

자바 바이트코드를 실행해주는 가상 실행 환경

- 자바 프로그램이 운영체제에서 바로 실행되는 것이 아니라, `.class` 형태의 바이트코드로 컴파일된 뒤 JVM 위에서 실행된다.
- JVM은 바이트코드를 읽고, 현재 운영체제와 하드웨어에 맞는 기계어로 변환하여 실행한다.
- 자바 프로그램은 운영체제와 직접 상호작용하지 않고 JVM을 통해 실행된다.
- 그래서 같은 바이트코드라면 Windows, macOS, Linux 등 여러 운영체제에서 실행될 수 있다.
- 단, JVM 자체는 운영체제 위에서 동작하므로 운영체제별 JVM이 따로 필요하다.

→ JVM은 운영체제별로 설치되어, 같은 자바 바이트코드를 각 운영체제에 맞게 실행해주는 엔진이다.

실행 흐름

```jsx
Java 코드(.java)
→ javac로 컴파일
→ 바이트코드(.class)
→ JVM이 실행
→ 운영체제에 맞는 기계어로 변환
→ 프로그램 실행
```

## JRE (Java Runtime Environment)

- JVM을 포함한 실행 패키지 (JVM 단독으로 프로그램 실행 불가)
- JRE = JVM + 표준 라이브러리 + 실행에 필요한 파일들
    
    *표준 라이브러리 ex) System.out.println, String, Scanner
    
- 개발이 아닌 실행만 한다면 JRE로 충분

## JDK (Java Development Kit)

- JDK = JRE + 개발도구(javac, jdb, javadoc, jar 등)
- 개발 도구가 전부 들어있는 가장 큰 묶음
- 자바 프로그램을 바이트코드로 컴파일해주는 javac컴파일러를 포함

**JVM⊂JRE⊂JDK**

JVM = 바이트코드를 실제로 실행하는 가상 머신

JRE = Java 프로그램 실행에 필요한 환경

JDK = Java 프로그램 개발에 필요한 도구 모음

!jdk_jre_jvm_nesting.svg

# add, commit, push

---

## add

수정한 파일을 **커밋할 준비 목록에 올리는 것**

## commit

add 해둔 변경 사항을 **하나의 저장 기록으로 남기는 것**

## push

내 컴퓨터에 저장된 commit 기록을 **GitHub 같은 원격 저장소에 올리는 것**

### * Git Bash로 로컬 폴더와 레포지토리 연동

```jsx
로컬 폴더 git bash로 open
git init 
git status 
git add . 
git commit -m “first commit”

<github 계정 연동 및 확인>
git config —global user.email “hch8344@naver.com”
git config —global user.name “HanChaehui”
git config —list 확인 후 q

<레포지토리 연결 및 확인>
git remote add origin https://github.com/HanChaehui/programmers-dev-lect.git
git push --set-upstream origin master (로컬 브랜치 `master`가 GitHub의 'master'와 연결))
git push
```

!commit.png

!add_commit_push.png